# Руководство по Admin Командам Мониторинга Каналов

## Содержание
1. [Обзор](#обзор)
2. [Установка](#установка)
3. [Команды](#команды)
4. [Примеры использования](#примеры-использования)
5. [Устранение неполадок](#устранение-неполадок)

---

## Обзор

Модуль `channel_admin.py` предоставляет команды для управления каналами-образцами, которые используются для анализа стиля контента. Эти каналы служат источниками вдохновения для AI при генерации контента.

### Что такое каналы-образцы?

Каналы-образцы — это публичные Telegram каналы, посты из которых анализируются системой для:
- Понимания стиля написания
- Извлечения образцов контента
- Обучения AI правильному тону и форматированию
- Добавления в RAG систему для улучшения генерации

### Основные возможности

- ✅ Добавление новых каналов для мониторинга
- ✅ Просмотр списка всех активных каналов
- ✅ Управление приоритетом и категориями
- ✅ Включение/отключение каналов
- ✅ Принудительная загрузка постов
- ✅ Просмотр детальной статистики
- ✅ Удаление каналов

---

## Установка

### 1. Проверка наличия файла

```bash
ls content_manager_bot/handlers/channel_admin.py
```

Файл должен быть на месте: `/content_manager_bot/handlers/channel_admin.py`

### 2. Проверка регистрации в main.py

```bash
grep "channel_admin_router" content_manager_bot/main.py
```

Должно быть:
```python
from content_manager_bot.handlers import ..., channel_admin_router
dp.include_router(channel_admin_router)
```

### 3. Проверка __init__.py

```bash
grep "channel_admin_router" content_manager_bot/handlers/__init__.py
```

Должно быть:
```python
from content_manager_bot.handlers.channel_admin import router as channel_admin_router
```

### 4. Запуск бота

Локально:
```bash
python -m content_manager_bot.main
```

На VPS:
```bash
systemctl restart nl-bots
journalctl -u nl-bots -f
```

---

## Команды

### Основные команды

| Команда | Описание | Синтаксис |
|---------|---------|----------|
| `/add_channel` | Добавить канал | `/add_channel @username [категория] [приоритет]` |
| `/list_channels` | Показать список | `/list_channels` |
| `/toggle_channel` | Включить/выключить | `/toggle_channel <id>` |
| `/fetch_now` | Загрузить посты | `/fetch_now [channel_id]` |
| `/channel_stats` | Статистика | `/channel_stats <id>` |
| `/remove_channel` | Удалить канал | `/remove_channel <id>` |

---

## Примеры использования

### Сценарий 1: Добавление каналов для мониторинга

**Шаг 1:** Определить, какие каналы нужно мониторить
```
Мотивация:
- @motivational_quotes
- @success_stories_ru
- @inspirational_daily

Продукты:
- @nl_products_official
- @products_showcase

Бизнес:
- @business_tips
- @entrepreneur_life
```

**Шаг 2:** Добавить каналы через бота

```
/add_channel @motivational_quotes motivation 9
/add_channel @success_stories_ru motivation 8
/add_channel @inspirational_daily motivation 7
/add_channel @nl_products_official product 10
/add_channel @products_showcase product 8
/add_channel @business_tips business 9
/add_channel @entrepreneur_life business 8
```

**Шаг 3:** Проверить список

```
/list_channels
```

Вывод:
```
📺 КАНАЛЫ ДЛЯ МОНИТОРИНГА

🟢 Motivational Quotes
   🔗 @motivational_quotes (ID: 1)
   💪 Категория: motivation
   ⭐⭐⭐⭐⭐⭐⭐⭐⭐ Приоритет: 9
   📊 Постов: 0
   ⏱ Не обновлялся

[... остальные каналы ...]

Всего каналов: 7
```

### Сценарий 2: Загрузка контента из каналов

**Шаг 1:** Загрузить из всех каналов

```
/fetch_now
```

Результат:
```
✅ Загрузка завершена!

📺 Обработано каналов: 7
📝 Новых постов: 145
📊 Всего в БД: 145

✅ Без ошибок
```

**Шаг 2:** Проверить конкретный канал

```
/channel_stats 1
```

Результат:
```
📊 СТАТИСТИКА КАНАЛА

📺 Motivational Quotes
🔗 @motivational_quotes
💪 Категория: motivation
⭐⭐⭐⭐⭐⭐⭐⭐⭐ Приоритет: 9
🟢 Статус: активен

📈 МЕТРИКИ:
• Всего постов: 25
• За последние 7 дней: 8
• С медиа: 18 (72%)
• Средние просмотры: 3450

⏱ Последнее обновление: 27.01.2026 14:30:15
📅 Последний пост: 27.01.2026 13:45
```

### Сценарий 3: Периодическое обновление

**График обновлений:**

```bash
# Каждый день в 02:00 UTC
0 2 * * * /usr/bin/curl -X POST http://localhost:8000/update_channels

# Или используя крон задачу на VPS:
# ssh root@194.87.86.103
# crontab -e
# 0 2 * * * curl -X POST http://localhost:8000/api/channels/fetch
```

**Вручную в боте:**

```
/fetch_now          # Обновить все
/fetch_now 1        # Обновить канал #1
```

### Сценарий 4: Управление приоритетами

**Проблема:** Один канал лучше других, нужно увеличить его приоритет

**Решение:**

```
/list_channels                    # Найти ID канала
/toggle_channel 3                 # Выключить старый канал
/add_channel @new_channel motivation 10   # Добавить с максимальным приоритетом
/list_channels                    # Проверить
```

### Сценарий 5: Ежедневная проверка

**Команды для проверки здоровья системы:**

```
/list_channels              # Все ли каналы активны?
/channel_stats 1            # Обновляется ли главный канал?
/fetch_now                  # Свежие посты загружаются?
```

---

## Категории каналов

Категория определяет тип контента, который хранится в канале:

| Категория | Эмодзи | Использование |
|-----------|--------|---|
| **motivation** | 💪 | Мотивационные посты, истории успеха, вдохновляющие цитаты |
| **product** | 🛍️ | Посты о продуктах, обзоры, характеристики, советы |
| **business** | 💼 | Бизнес-контент, стартапы, предпринимательство, статьи |
| **lifestyle** | 🎨 | Лайфстайл контент, образ жизни, здоровье, путешествия |
| **general** | 📺 | По умолчанию, если категория не подходит |

**Рекомендация:** Используйте категории из этого списка, они оптимизированы для анализа стиля.

---

## Приоритеты

Приоритет определяет важность канала при анализе стиля (1-10):

```
⭐         = Приоритет 1  (минимальный)
⭐⭐       = Приоритет 2
⭐⭐⭐     = Приоритет 3
⭐⭐⭐⭐   = Приоритет 4
⭐⭐⭐⭐⭐ = Приоритет 5  (средний)
...
⭐⭐⭐⭐⭐⭐⭐⭐⭐⭐ = Приоритет 10 (максимальный)
```

**Рекомендации:**

- **7-10:** Главные образцы стиля (лучшие каналы, которым мы доверяем)
- **5-6:** Средние источники (хорошие примеры)
- **1-4:** Дополнительные источники (для разнообразия)

**Пример:**

```
/add_channel @official_channel motivation 10    # Главный образец
/add_channel @good_channel motivation 7         # Хороший источник
/add_channel @additional_channel motivation 4   # Дополнительный
```

---

## Статистика и мониторинг

### Что отслеживается?

Для каждого канала система сохраняет:

- ✅ **Количество постов** — всего загружено
- ✅ **Посты за неделю** — активность последние 7 дней
- ✅ **Посты с медиа** — процент постов с фото/видео
- ✅ **Среднее количество просмотров** — популярность контента
- ✅ **Дата последнего обновления** — свежесть данных
- ✅ **Дата последнего поста** — активность канала

### Когда смотреть статистику?

```
/channel_stats <id>
```

**Хорошие показатели:**
- Постов за неделю: > 2
- С медиа: > 50%
- Среднее просмотров: > 1000
- Обновлено: < 24 часов назад

**Плохие показатели (может быть проблема):**
- Обновлено: > 7 дней назад
- Постов: 0
- Статус: выключен (🔴)

---

## Устранение неполадок

### Проблема: "❌ Не удалось добавить канал"

**Причины:**
1. Канал не существует → проверить юзернейм
2. Канал приватный → использовать публичный
3. Канал уже добавлен → использовать `/list_channels`
4. Не настроены Telethon credentials → добавить в .env

**Решение:**

```bash
# 1. Проверить .env
grep TELETHON .env

# 2. Если не заполнены - получить на https://my.telegram.org/apps
# Добавить в .env:
export TELETHON_API_ID=123456
export TELETHON_API_HASH=abcdef123456...

# 3. Перезапустить бот
systemctl restart nl-bots
```

### Проблема: Команды не работают

**Решение:**

1. Проверить, что вы администратор:
```bash
grep ADMIN_TELEGRAM_IDS .env
# Ваш ID должен быть в списке
```

2. Проверить логи:
```bash
journalctl -u nl-bots -n 50 -f
```

3. Проверить регистрацию роутера:
```bash
grep "channel_admin" content_manager_bot/main.py
```

### Проблема: Посты не загружаются

**Решение:**

```bash
# 1. Проверить, что каналы активны
/list_channels         # Все ли имеют 🟢?

# 2. Проверить сетевое соединение
ping 8.8.8.8

# 3. Проверить логи
journalctl -u nl-bots -f | grep "style\|channel"

# 4. Попробовать загрузить руками
/fetch_now 1           # Загрузить канал #1
```

### Проблема: Очень медленная загрузка

**Причины:**
- Интернет медленный
- Много постов в канале
- Канал имеет много медиа

**Решение:**

```
# Загружать меньше постов
/fetch_now              # По умолчанию 50 постов

# Или загружать в фоне (не ждать результата)
curl -X POST http://localhost:8000/api/channels/fetch
```

---

## API для интеграции

Если нужна автоматизация, можно вызывать команды через API:

```python
# Например, из другого скрипта
import aiohttp

async def add_channel(username, category):
    async with aiohttp.ClientSession() as session:
        await session.post(
            'http://localhost:8000/api/channels/add',
            json={'username': username, 'category': category}
        )
```

---

## Лучшие практики

### 1. Регулярное обновление
```
Запускайте /fetch_now каждый день в одно время
Например: 02:00 UTC
```

### 2. Монитор здоровья
```
Раз в неделю проверяйте:
/list_channels
/channel_stats <top_channel_id>
```

### 3. Архивирование старых каналов
```
Если канал не обновляется > 30 дней:
/toggle_channel <id>    # Выключить вместо удаления
```

### 4. Балансировка приоритетов
```
Пересматривайте приоритеты раз в месяц
Лучшие каналы → приоритет 8-10
Средние → 5-7
Старые → 1-3 или удалить
```

### 5. Категоризация
```
Четко разделяйте каналы по категориям
Не смешивайте motivation с product в одном канале
```

---

## Версия

- **Версия:** 1.0
- **Дата создания:** 27 января 2026
- **Автор:** Claude Code
- **Совместимость:** aiogram 3.x, Python 3.9+

## Лицензия

Часть проекта NL International AI Bots

## Контакты

При возникновении вопросов — напишите администратору бота.

---

## Чек-лист для администратора

- [ ] Установлены все файлы
- [ ] Зарегистрированы роутеры
- [ ] Настроены Telethon credentials
- [ ] Добавлены каналы для мониторинга
- [ ] Первая загрузка постов выполнена
- [ ] Статистика отображается корректно
- [ ] Проверена регулярная загрузка

---

## Дополнительные ресурсы

- `CHANNEL_ADMIN_COMMANDS.md` — справочник по каждой команде
- `PROJECT_ANALYSIS_2026_01_23.md` — общее описание системы
- Логи бота: `journalctl -u nl-bots -f`

