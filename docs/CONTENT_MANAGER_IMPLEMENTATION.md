# AI-Контент-Менеджер - Документация реализации

**Дата создания:** 2026-01-16
**Статус:** РЕАЛИЗОВАНО
**Агент:** Claude (Opus 4.5)

---

## Обзор

AI-Контент-менеджер (`@nl_content_bot`) - второй бот в системе NL International AI Bots, предназначенный для автоматической генерации и публикации контента в Telegram канал.

## Функциональность

### Основные возможности:
1. **Автоматическая генерация постов** - AI создаёт посты для Telegram канала через GigaChat
2. **Модерация контента** - посты показываются администратору на одобрение
3. **Публикация** - после одобрения пост публикуется в канал
4. **Планирование** - возможность запланировать публикацию на определённое время
5. **Редактирование** - AI может отредактировать пост по инструкциям админа
6. **Перегенерация** - создание нового варианта с учётом фидбека

### Типы контента:
| Тип | Описание |
|-----|----------|
| `product` | Информация о продуктах NL International |
| `motivation` | Мотивационные посты для партнёров |
| `news` | Новости компании |
| `tips` | Советы по продажам и работе с клиентами |
| `success_story` | Истории успеха партнёров |
| `promo` | Акции и специальные предложения |

---

## Архитектура

```
content_manager_bot/
├── __init__.py
├── main.py                    # Точка входа бота
├── ai/
│   ├── __init__.py
│   ├── content_generator.py   # Генератор контента через GigaChat
│   └── prompts.py             # Промпты для разных типов постов
├── database/
│   ├── __init__.py
│   └── models.py              # Модели: Post, ContentSchedule, AdminAction
├── handlers/
│   ├── __init__.py
│   ├── admin.py               # Команды для администратора
│   └── callbacks.py           # Обработка кнопок и FSM состояний
├── scheduler/
│   ├── __init__.py
│   └── content_scheduler.py   # Планировщик публикаций
├── utils/
│   ├── __init__.py
│   └── keyboards.py           # Inline клавиатуры
├── analytics/
│   └── __init__.py            # (в разработке)
└── scraper/
    └── __init__.py            # (в разработке)
```

---

## Модели базы данных

### Post (Пост)
```python
class Post(Base, TimestampMixin):
    __tablename__ = "content_posts"

    id: int (PK)
    content: str                 # Текст поста
    post_type: str               # product, motivation, news, tips, success_story, promo
    status: str                  # draft, pending, approved, published, rejected, scheduled
    generated_at: datetime
    approved_at: datetime (nullable)
    published_at: datetime (nullable)
    scheduled_for: datetime (nullable)
    channel_message_id: int (nullable)  # ID сообщения в канале
    admin_id: int (nullable)     # Кто одобрил
    rejection_reason: str (nullable)
    ai_model: str                # Использованная AI модель
    prompt_used: str (nullable)  # Использованный промпт
    generation_params: JSONB     # Параметры генерации
    views_count: int (nullable)  # Метрики
    reactions_count: int (nullable)
```

### ContentSchedule (Расписание генерации)
```python
class ContentSchedule(Base, TimestampMixin):
    __tablename__ = "content_schedules"

    id: int (PK)
    post_type: str               # Тип поста для генерации
    cron_expression: str         # Расписание в формате cron
    is_active: bool
    last_run: datetime
    next_run: datetime
    total_generated: int         # Статистика
    total_published: int
```

### AdminAction (Лог действий)
```python
class AdminAction(Base, TimestampMixin):
    __tablename__ = "content_admin_actions"

    id: int (PK)
    admin_id: int                # Telegram ID админа
    post_id: int (nullable)
    action: str                  # generate, approve, reject, publish, edit, regenerate
    details: JSONB               # Дополнительные данные
```

---

## Команды администратора

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и информация о боте |
| `/generate` | Показать меню выбора типа поста |
| `/generate <тип>` | Сгенерировать пост определённого типа |
| `/pending` | Показать посты на модерации (до 10) |
| `/stats` | Статистика публикаций |
| `/schedule` | Настройки автоматической генерации |
| `/help` | Справка по командам |

---

## Флоу работы

### 1. Ручная генерация поста
```
Админ: /generate (или /generate product)
    ↓
Бот: Показывает меню выбора типа (или сразу генерирует)
    ↓
Бот: Отправляет запрос в GigaChat
    ↓
Бот: Сохраняет пост в БД со статусом "pending"
    ↓
Бот: Показывает пост админу с кнопками:
     [✅ Опубликовать] [📅 Запланировать]
     [📝 Редактировать] [🔄 Перегенерировать]
     [❌ Отклонить]
    ↓
Админ: Нажимает кнопку
    ↓
Бот: Выполняет действие
```

### 2. Планирование публикации
```
Админ: Нажимает [📅 Запланировать]
    ↓
Бот: Показывает варианты времени:
     [Через 1 час] [Через 3 часа]
     [Завтра 9:00] [Завтра 18:00]
     [Указать время]
    ↓
Админ: Выбирает время
    ↓
Бот: Меняет статус на "scheduled", сохраняет время
    ↓
Scheduler: Автоматически публикует в указанное время
```

### 3. Редактирование через AI
```
Админ: Нажимает [📝 Редактировать]
    ↓
Бот: Просит ввести инструкции
    ↓
Админ: "Сделай короче и добавь эмодзи"
    ↓
Бот: Отправляет в GigaChat с инструкциями
    ↓
Бот: Показывает обновлённый пост на модерацию
```

### 4. Автоматическая генерация (Scheduler)
```
Scheduler: Каждую минуту проверяет:
    1. Есть ли посты для публикации (scheduled_for <= now)
    2. Есть ли активные расписания (next_run <= now)
    ↓
При наступлении времени:
    - Публикует scheduled посты в канал
    - Генерирует новые посты по расписанию (статус pending)
    - Уведомляет админов
```

---

## Интеграции

### GigaChat (Сбер)
- Используется тот же клиент что и в AI-Кураторе
- Модуль: `shared/ai_clients/gigachat_client.py`
- Токен: `GIGACHAT_AUTH_TOKEN` из `.env`
- Бесплатный тариф!

### Telegram Channel
- Публикация через Bot API (`bot.send_message`)
- Канал указан в `CHANNEL_USERNAME`
- Бот должен быть администратором канала

---

## Конфигурация (.env)

```env
# Content Manager Bot
CONTENT_MANAGER_BOT_TOKEN=your_bot_token_from_botfather
CHANNEL_USERNAME=@your_channel_username

# AI (общее с куратором)
GIGACHAT_AUTH_TOKEN=your_gigachat_base64_token
CONTENT_MANAGER_AI_MODEL=GigaChat

# Admins (общее)
ADMIN_TELEGRAM_IDS=123456789,987654321

# Database (общее)
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/nl_bots
```

---

## Запуск

```bash
# Из корня проекта
python -m content_manager_bot.main
```

---

## Статус реализации

- [x] Документация и план
- [x] Модели базы данных (models.py)
- [x] Генератор контента (content_generator.py)
- [x] Промпты для постов (prompts.py)
- [x] Админ хендлеры (admin.py)
- [x] Callback обработчики (callbacks.py)
- [x] Планировщик (content_scheduler.py)
- [x] Клавиатуры (keyboards.py)
- [x] Главный файл (main.py)
- [ ] Создание бота в BotFather
- [ ] Добавление токена в .env
- [ ] Создание таблиц в БД
- [ ] Тестирование

---

## Следующие шаги для запуска

1. **Создать бота в Telegram:**
   - Открыть @BotFather
   - `/newbot`
   - Имя: `NL Content Manager`
   - Username: `nl_content_bot`
   - Сохранить токен

2. **Добавить в .env:**
   ```
   CONTENT_MANAGER_BOT_TOKEN=полученный_токен
   CHANNEL_USERNAME=@ваш_канал
   ```

3. **Создать таблицы БД:**
   ```bash
   python scripts/create_database.py
   ```

4. **Добавить бота админом канала**

5. **Запустить:**
   ```bash
   python -m content_manager_bot.main
   ```

---

## Заметки для других агентов

1. **Структура проекта** полностью реализована
2. **GigaChat клиент** общий с куратором в `shared/ai_clients/`
3. **База данных** PostgreSQL + SQLAlchemy 2.0 async
4. **Aiogram 3.x** с FSM для состояний редактирования
5. **Scheduler** работает в фоне, проверяет каждую минуту
6. **Все действия** логируются в таблицу `content_admin_actions`

---

## Известные ограничения

1. Автопостинг пока с простой логикой (каждые 24 часа)
2. Нет полноценного парсинга cron выражений
3. Метрики (просмотры, реакции) пока не собираются автоматически
4. Нет интеграции с аналитикой канала
