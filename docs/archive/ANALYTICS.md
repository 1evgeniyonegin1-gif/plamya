# Система аналитики постов

## Описание

Система аналитики собирает и анализирует метрики постов из Telegram:
- Просмотры (views)
- Реакции (reactions)
- Пересылки (forwards)
- Коэффициент вовлеченности (engagement rate)

## Архитектура

### Модели данных

#### Post (расширенная)
Основная модель поста с добавленными полями метрик:
```python
views_count: int           # Количество просмотров
reactions_count: int       # Количество реакций
forwards_count: int        # Количество пересылок
reactions_breakdown: dict  # Разбивка реакций по типам {"👍": 10, "❤️": 5}
engagement_rate: float     # Коэффициент вовлеченности (%)
last_metrics_update: datetime  # Время последнего обновления
```

#### PostAnalytics
Исторические снимки метрик для отслеживания динамики:
```python
post_id: int              # ID поста
channel_message_id: int   # ID сообщения в Telegram
views_count: int          # Просмотры на момент снимка
reactions_count: int      # Реакции на момент снимка
forwards_count: int       # Пересылки на момент снимка
reactions_breakdown: dict # Детальная разбивка реакций
snapshot_at: datetime     # Время снимка
views_delta: int          # Прирост просмотров
reactions_delta: int      # Прирост реакций
```

### Модули

#### StatsCollector (`content_manager_bot/analytics/stats_collector.py`)
Сервис сбора статистики из Telegram API:
- `collect_post_stats(post)` - собрать метрики для поста
- `update_post_metrics(post)` - обновить метрики и создать снимок
- `update_all_published_posts()` - обновить все опубликованные посты
- `get_post_growth(post_id, hours)` - получить прирост за период

#### AnalyticsService (`content_manager_bot/analytics/analytics_service.py`)
Сервис анализа данных и генерации отчетов:
- `get_overall_stats(days)` - общая статистика за период
- `get_stats_by_type(days)` - статистика по типам контента
- `get_top_posts(limit, days, sort_by)` - топ постов
- `get_reactions_distribution(days)` - распределение реакций
- `format_dashboard(days)` - форматированный дашборд

## Команды бота

### /stats
Базовая статистика:
- Всего сгенерировано постов
- Опубликовано
- На модерации
- Отклонено

### /analytics [дней]
Детальная аналитика за период:
```
/analytics      # за 7 дней (по умолчанию)
/analytics 30   # за 30 дней
```

Показывает:
- Общие метрики (посты, просмотры, реакции, пересылки)
- Средние показатели на пост
- Статистику по типам контента
- Топ-5 постов по вовлеченности

### /update_stats
Принудительное обновление статистики всех постов из Telegram.

Автоматически создает исторические снимки метрик.

### /top [критерий] [количество] [дней]
Топ постов по заданному критерию:
```
/top                    # топ-10 по вовлеченности за 30 дней
/top views              # топ-10 по просмотрам
/top reactions 5 7      # топ-5 по реакциям за 7 дней
/top engagement 20 90   # топ-20 по вовлеченности за 90 дней
```

Критерии сортировки:
- `views` - просмотры
- `reactions` - реакции
- `engagement` - коэффициент вовлеченности

## Автоматическое обновление

Планировщик (`ContentScheduler`) автоматически обновляет статистику:
- **Периодичность**: каждые 30 минут
- **Метод**: `_update_stats_if_needed()`
- **Логика**:
  1. Проверяет время последнего обновления
  2. Если прошло >30 минут, запускает сбор
  3. Обновляет метрики всех опубликованных постов
  4. Создает исторические снимки

## Интерактивные кнопки

Клавиатура аналитики (`Keyboards.analytics_menu()`):
```
[📊 Аналитика за 7 дней] [📊 За 30 дней]
[🏆 Топ по просмотрам] [❤️ Топ по реакциям]
[📈 Топ по вовлеченности]
[🔄 Обновить статистику]
```

## Метрики

### Engagement Rate (Коэффициент вовлеченности)
Формула:
```python
engagement_rate = ((reactions + forwards) / views) * 100
```

Показывает процент пользователей, которые взаимодействовали с постом.

**Интерпретация:**
- 0-1% - низкая вовлеченность
- 1-3% - средняя вовлеченность
- 3-5% - хорошая вовлеченность
- 5%+ - отличная вовлеченность

### Views (Просмотры)
Количество уникальных просмотров поста.

**Примечание:** Для групп с Topics views может быть недоступен (Telegram API ограничение).

### Reactions (Реакции)
Сумма всех реакций на пост.

`reactions_breakdown` содержит разбивку по эмодзи:
```json
{
  "👍": 10,
  "❤️": 5,
  "🔥": 3,
  "😍": 2
}
```

### Forwards (Пересылки)
Количество раз, когда пост был переслан.

## Миграция БД

Для применения изменений в схеме БД:

```bash
python scripts/migrate_analytics.py
```

Миграция добавляет:
1. Новые поля в `content_posts`:
   - `forwards_count`
   - `reactions_breakdown`
   - `engagement_rate`
   - `last_metrics_update`

2. Новую таблицу `content_post_analytics`

3. Индексы для оптимизации запросов

## Примеры использования

### Программный доступ

```python
from content_manager_bot.analytics import StatsCollector, AnalyticsService

# Сбор статистики
async with AsyncSessionLocal() as session:
    collector = StatsCollector(bot, session)

    # Обновить конкретный пост
    await collector.update_post_metrics(post)

    # Обновить все посты
    count = await collector.update_all_published_posts()

    # Получить прирост за 24 часа
    growth = await collector.get_post_growth(post_id=123, hours=24)

# Анализ данных
async with AsyncSessionLocal() as session:
    analytics = AnalyticsService(session)

    # Общая статистика
    stats = await analytics.get_overall_stats(days=30)

    # Топ постов
    top = await analytics.get_top_posts(limit=10, days=7, sort_by='engagement')

    # Дашборд
    dashboard = await analytics.format_dashboard(days=7)
```

## Ограничения

1. **Views в группах**: Telegram API не всегда предоставляет просмотры для сообщений в группах (только в каналах)

2. **Частота обновления**: Слишком частые запросы могут привести к rate limit от Telegram API

3. **Исторические данные**: Снимки создаются только после первого обновления, исторические данные до внедрения системы недоступны

## Roadmap

- [ ] Графики динамики метрик
- [ ] A/B тестирование типов контента
- [ ] Прогнозирование лучшего времени публикации
- [ ] Экспорт отчетов в CSV/Excel
- [ ] Интеграция с внешними системами аналитики
- [ ] Сравнение эффективности с/без изображений
- [ ] Анализ влияния времени суток на вовлеченность
