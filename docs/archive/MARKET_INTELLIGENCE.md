# Система Маркетинговой Аналитики (Market Intelligence)

## Что это?

Автоматическая система сбора и обновления информации о:
- Новостях NL International
- Конкурентах в нише MLM/нутрициология
- Трендах в индустрии
- Актуальных промо и акциях

Данные автоматически добавляются в RAG базу знаний, чтобы куратор всегда был в курсе последних событий.

---

## Архитектура

```
┌─────────────────────────────────────────┐
│   Источники данных                      │
├─────────────────────────────────────────┤
│ • nl-international.ru (новости)         │
│ • Конкуренты (Herbalife, Siberian...)   │
│ • Отраслевые тренды (нутрициология)     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Сборщик (MarketIntelligenceCollector) │
├─────────────────────────────────────────┤
│ • Парсинг новостей                       │
│ • Анализ конкурентов                     │
│ • Сбор трендов                           │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   RAG База Знаний (Vector Store)        │
├─────────────────────────────────────────┤
│ • Векторные embeddings                   │
│ • Метаданные (дата, категория)          │
│ • Автоочистка старых данных (30 дней)   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   AI-Куратор                             │
├─────────────────────────────────────────┤
│ • Использует свежие данные в ответах    │
│ • Знает актуальные новости NL           │
│ • Понимает рыночную ситуацию            │
└─────────────────────────────────────────┘
```

---

## Установка на VPS

### 1. Скопировать файлы systemd

```bash
# Подключиться к серверу
ssh root@194.87.86.103

# Перейти в директорию проекта
cd /root/nl-international-ai-bots

# Скопировать systemd файлы
sudo cp scripts/systemd/market-intelligence.service /etc/systemd/system/
sudo cp scripts/systemd/market-intelligence.timer /etc/systemd/system/

# Перезагрузить systemd
sudo systemctl daemon-reload
```

### 2. Включить и запустить таймер

```bash
# Включить автозапуск
sudo systemctl enable market-intelligence.timer

# Запустить таймер
sudo systemctl start market-intelligence.timer

# Проверить статус
sudo systemctl status market-intelligence.timer
```

### 3. Проверить что работает

```bash
# Посмотреть когда следующий запуск
systemctl list-timers market-intelligence.timer

# Запустить вручную для теста
sudo systemctl start market-intelligence.service

# Посмотреть логи
journalctl -u market-intelligence.service -f
```

---

## Расписание

По умолчанию скрипт запускается:
- **Каждый день в 06:00 UTC** (09:00 по Москве)

### Изменить расписание

Отредактировать файл `/etc/systemd/system/market-intelligence.timer`:

```ini
# Запуск каждые 12 часов
OnCalendar=*-*-* 00,12:00:00

# Запуск каждый понедельник в 08:00
OnCalendar=Mon *-*-* 08:00:00

# Запуск каждый час
OnCalendar=hourly
```

После изменений:
```bash
sudo systemctl daemon-reload
sudo systemctl restart market-intelligence.timer
```

---

## Источники данных

### NL International
- **URL:** https://nl-international.ru/news/
- **Что собираем:** Новости, новинки продуктов, акции

### Конкуренты
1. **Herbalife Russia**
   - Основной конкурент в сегменте функционального питания
   - Отслеживаем: новинки, маркетинг, промо

2. **Siberian Wellness (Сибирское Здоровье)**
   - Российский MLM в нутрициологии
   - Отслеживаем: тренды, позиционирование

3. **Oriflame Wellness**
   - Wellness линейка Oriflame
   - Отслеживаем: стратегии продвижения

### Отраслевые тренды
- Нутрициология: новые исследования, тренды
- Функциональное питание: что популярно в 2026
- Сетевой маркетинг: изменения в индустрии
- БАДы: новые регуляции, исследования

---

## Как это работает

### 1. Сбор данных

Скрипт `update_market_intelligence.py`:
1. Парсит новости с сайта NL
2. Ищет информацию о конкурентах
3. Собирает тренды индустрии
4. Структурирует данные с метаданными

### 2. Сохранение в RAG

Каждый документ сохраняется с метаданными:
```python
{
    "content": "Текст новости...",
    "metadata": {
        "date": "2026-01-20",
        "category": "nl_news",  # или competitor_insights, industry_trends
        "source": "nl-international.ru",
        "relevance": "high"  # high, medium, low
    }
}
```

### 3. Автоочистка

Данные старше **30 дней** автоматически удаляются, чтобы база оставалась актуальной.

### 4. Использование куратором

Когда партнёр спрашивает куратора:
- "Какие новости у NL?"
- "Что делают конкуренты?"
- "Какие тренды в нутрициологии?"

RAG система находит свежие данные и куратор отвечает со знанием дела.

---

## Логи

### Просмотр логов

```bash
# Все логи сервиса
journalctl -u market-intelligence.service

# Последние 50 строк
journalctl -u market-intelligence.service -n 50

# Логи в реальном времени
journalctl -u market-intelligence.service -f

# Логи за сегодня
journalctl -u market-intelligence.service --since today
```

### Файл логов

Также пишется в: `/root/nl-international-ai-bots/logs/market_intelligence.log`

```bash
# Посмотреть последние записи
tail -f /root/nl-international-ai-bots/logs/market_intelligence.log
```

---

## Ручной запуск

Для тестирования можно запустить вручную:

```bash
cd /root/nl-international-ai-bots
source venv/bin/activate
python scripts/update_market_intelligence.py
```

---

## Troubleshooting

### Таймер не запускается

```bash
# Проверить статус
systemctl status market-intelligence.timer

# Посмотреть ошибки
journalctl -u market-intelligence.timer -n 20

# Перезапустить
sudo systemctl restart market-intelligence.timer
```

### Скрипт падает с ошибкой

```bash
# Посмотреть последнюю ошибку
journalctl -u market-intelligence.service -n 50

# Запустить вручную для отладки
cd /root/nl-international-ai-bots
source venv/bin/activate
python scripts/update_market_intelligence.py
```

### Нет доступа к WebSearch

Если WebSearch недоступен, нужно:
1. Проверить настройки API
2. Использовать альтернативные источники (RSS, Telegram каналы)
3. Временно использовать заглушки

---

## Расширение функционала

### Добавить новый источник

1. Открыть `scripts/update_market_intelligence.py`
2. Добавить в `self.sources`:

```python
"new_source": {
    "name": "Название источника",
    "url": "https://example.com",
    "enabled": True
}
```

3. Добавить метод сбора:

```python
async def _collect_from_new_source(self) -> List[str]:
    # Логика парсинга
    return documents
```

4. Вызвать в `collect_all()`

### Изменить частоту обновления

См. раздел "Изменить расписание" выше.

### Добавить уведомления

Можно добавить отправку уведомлений в Telegram при обнаружении важных новостей:

```python
# В конце main()
if important_news:
    await send_telegram_notification(admin_id, summary)
```

---

## TODO (что доработать)

- [ ] Реализовать реальный парсинг сайта NL
- [ ] Подключить WebSearch для трендов
- [ ] Добавить метаданные в векторную БД
- [ ] Реализовать автоочистку старых данных
- [ ] Добавить RSS парсинг как альтернативу
- [ ] Мониторинг Telegram каналов конкурентов
- [ ] Уведомления админу о важных новостях
- [ ] Дашборд с статистикой собранных данных

---

## Статус

**Текущая версия:** 1.0 (базовая архитектура)

**Что работает:**
- ✅ Структура скрипта
- ✅ Systemd таймер
- ✅ Логирование
- ✅ Генерация отчетов

**Что в разработке:**
- 🔨 Реальный парсинг источников
- 🔨 Интеграция с RAG
- 🔨 Автоочистка старых данных
