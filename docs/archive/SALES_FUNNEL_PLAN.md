# План доработки бота под автоворонку продаж

**Дата:** 21 января 2026
**Статус:** ✅ РЕАЛИЗОВАНО (см. SALES_FUNNEL_IMPLEMENTATION.md)
**Приоритет:** ВЫСОКИЙ

> **ВАЖНО:** Этот план был полностью реализован 21 января 2026.
> Документация по реализации: `docs/SALES_FUNNEL_IMPLEMENTATION.md`

---

## Текущая ситуация

### Что есть сейчас:
- AI-Куратор отвечает на вопросы о продуктах NL
- База знаний RAG (56 документов)
- Экспертные консультации по нутрициологии
- Информация о маркетинг-плане

### Чего НЕТ (критично для воронки):
- Воронка квалификации лида
- Предложение продукта с реферальной ссылкой
- Путь регистрации партнёра
- Сбор контактов (телефон/email)
- Автоматические напоминания
- CRM-функционал

---

## Целевая модель: Автоворонка

```
┌─────────────────────────────────────────────────────────────────┐
│                    ВОРОНКА ПРОДАЖ                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   Reels / TikTok / Shorts (боли: здоровье, похудение, энергия)  │
│                              ↓                                   │
│                    CTA: "Бесплатная консультация"                │
│                              ↓                                   │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                  @nl_curator_bot                         │   │
│   │                                                          │   │
│   │  /start → Квалификация → Выявление боли → Рекомендация  │   │
│   │                              ↓                           │   │
│   │         ┌─────────────┬─────────────┐                   │   │
│   │         ↓             ↓             ↓                   │   │
│   │     [КЛИЕНТ]     [БИЗНЕС]    [ПРОСТО СМОТРЮ]           │   │
│   │         ↓             ↓             ↓                   │   │
│   │   Прогрев 3-5    Прогрев 5-7   Контент +               │   │
│   │   сообщений      сообщений    напоминания              │   │
│   │         ↓             ↓                                 │   │
│   │   Продукт +      Расчёт дохода                         │   │
│   │   реф.ссылка     + реф.ссылка                          │   │
│   │         ↓             ↓                                 │   │
│   │      ЗАКАЗ      РЕГИСТРАЦИЯ                            │   │
│   │                  ПАРТНЁРА                               │   │
│   └─────────────────────────────────────────────────────────┘   │
│                              ↓                                   │
│                    💰 Комиссия на твой ID                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Этапы реализации

### ЭТАП 1: Воронка квалификации (8-10 часов)

#### 1.1 Новый /start с выбором пути

**Файл:** `curator_bot/handlers/commands.py`

**Текущее поведение:**
```
/start → Приветствие → Свободный диалог
```

**Новое поведение:**
```
/start → Приветствие → Inline-кнопки выбора
    ↓
[🍎 Хочу улучшить здоровье]
[💼 Интересует заработок]
[❓ Просто хочу узнать больше]
```

**Что нужно:**
- Создать inline-клавиатуру с 3 кнопками
- Сохранять выбор пользователя в БД (поле `user_intent`)
- Направлять на соответствующую ветку диалога

#### 1.2 Выявление боли (для клиентов)

**После выбора "Здоровье":**
```
Бот: "Отлично! Что тебя интересует больше всего?"

[🔥 Похудеть / контроль веса]
[⚡ Больше энергии]
[💪 Укрепить иммунитет]
[✨ Красота: кожа, волосы, ногти]
[🧒 Здоровье детей]
[🏃 Спорт и восстановление]
```

**Что нужно:**
- Callback-обработчики для каждой кнопки
- Сохранение выбранной боли в БД (поле `pain_point`)
- Переход к персональной рекомендации

#### 1.3 Выявление интереса (для бизнеса)

**После выбора "Заработок":**
```
Бот: "Круто, что думаешь о дополнительном доходе!
      Что для тебя важнее?"

[💰 Просто подработка 10-30к/мес]
[📈 Серьёзный доход 50-100к/мес]
[🚀 Хочу построить бизнес 200к+/мес]
[🤔 Пока не уверен, расскажи подробнее]
```

**Что нужно:**
- Callback-обработчики для бизнес-пути
- Сохранение уровня амбиций в БД (поле `income_goal`)
- Переход к презентации возможностей

---

### ЭТАП 2: Цепочки прогрева (10-12 часов)

#### 2.1 Прогрев для клиентов (3-5 сообщений)

**Структура цепочки "Похудение":**

```
Сообщение 1 (сразу):
"Окей, давай по факту про похудение.

Главное правило: дефицит калорий. Всё остальное — детали.
Но есть нюанс: если просто меньше есть — организм замедляет метаболизм.

Нужен баланс: достаточно белка + контроль калорий + нутриенты.

Сейчас расскажу, как это работает на практике..."

[Продолжить →]
```

```
Сообщение 2 (после клика):
"Energy Diet — это не волшебная таблетка.
Это инструмент контроля.

Одна порция = 200 ккал, при этом:
• 25г белка (сытость на 3-4 часа)
• Все витамины и минералы
• Готовится за 2 минуты

Заменяешь 1-2 приёма пищи → автоматический дефицит калорий.
Без голода, без подсчёта, без срывов.

Хочешь узнать, какой вариант подойдёт именно тебе?"

[Да, подбери для меня]
[Сколько это стоит?]
```

```
Сообщение 3 (после "Да, подбери"):
"Чтобы подобрать оптимальный вариант:

Сколько хочешь сбросить?
[5-10 кг]
[10-20 кг]
[Больше 20 кг]
[Хочу поддерживать вес]
```

```
Сообщение 4 (персональная рекомендация):
"Для твоей цели идеально подойдёт:

🥤 Energy Diet Smart — стартовый набор
• 5 вкусов на пробу
• 15 порций = 2 недели
• Цена: [X] ₽ (со скидкой 25%)

Это оптимальный старт, чтобы понять — подходит тебе или нет.

👉 Заказать со скидкой: [ССЫЛКА]

Или напиши мне любой вопрос — отвечу!"
```

#### 2.2 Прогрев для бизнеса (5-7 сообщений)

**Структура цепочки "Заработок 50-100к":**

```
Сообщение 1:
"50-100к в месяц — реальная цель. Расскажу как это работает.

В NL International есть 4 потока дохода:
1. Личный объём (продажи на свой ID)
2. Партнёрский бонус (за активность команды)
3. Групповой объём (% от оборота структуры)
4. Клубные бонусы (от оборота всей компании)

Для 50-100к нужна квалификация M3 или B1.
Сейчас покажу конкретные цифры..."

[Показать расчёт →]
```

```
Сообщение 2:
"Квалификация M3 (Middle 3):
• Групповой объём: 3,000 PV
• Вознаграждение: 24% = 25,200 ₽/мес

Как набрать 3,000 PV:
• Твой ЛО: 200 PV (свои покупки)
• 10 клиентов по 70 PV = 700 PV
• 5 партнёров с ЛО 200 PV = 1,000 PV
• Глубина структуры: +1,100 PV

Итого: ~25,000 ₽ с ГО + партнёрский бонус ~10,000 ₽
= 35,000 ₽/мес

Хочешь узнать, как выйти на 50-100к?"

[Да, расскажи →]
```

```
Сообщение 3:
"Для 50-100к нужна квалификация B1-B2.

B1 (Business Partner 1):
• ГО 5,500 PV + 1 партнёр M3 в первой линии
• Доход: 50,000-70,000 ₽/мес

B2 (Business Partner 2):
• ГО 8,000 PV + 2 партнёра M3
• Доход: 80,000-120,000 ₽/мес

Ключ — найти 1-2 человека, которые тоже хотят строить.
Ты их обучаешь → они растут до M3 → ты получаешь бонусы с их команды.

Готов попробовать?"

[Да, хочу зарегистрироваться]
[Есть вопросы]
```

```
Сообщение 4 (после "Да"):
"Отлично! Вот что нужно для старта:

1. Регистрация по ссылке (бесплатно):
   👉 [РЕФЕРАЛЬНАЯ ССЫЛКА]

2. Первый заказ 70 PV (стартовый набор ~3,500 ₽)
   Это активирует твой контракт

3. Я добавлю тебя в обучение
   Всё расскажу и помогу с первыми шагами

После регистрации напиши мне — дам доступ к материалам!"
```

#### 2.3 Что нужно реализовать:

**Новые файлы:**
```
curator_bot/
├── funnels/
│   ├── __init__.py
│   ├── client_funnel.py      # Цепочка для клиентов
│   ├── business_funnel.py    # Цепочка для бизнеса
│   ├── messages.py           # Тексты всех сообщений
│   └── keyboards.py          # Inline-клавиатуры
```

**Изменения в БД:**
```sql
ALTER TABLE users ADD COLUMN user_intent VARCHAR(50);     -- client/business/curious
ALTER TABLE users ADD COLUMN pain_point VARCHAR(100);     -- weight/energy/immunity...
ALTER TABLE users ADD COLUMN income_goal VARCHAR(50);     -- 10-30k/50-100k/200k+
ALTER TABLE users ADD COLUMN funnel_step INTEGER DEFAULT 0;
ALTER TABLE users ADD COLUMN funnel_started_at TIMESTAMP;
```

**Callback-обработчики:**
```python
# curator_bot/handlers/callbacks.py

@router.callback_query(F.data == "intent_client")
async def handle_client_intent(callback: CallbackQuery):
    # Сохранить intent, показать выбор боли

@router.callback_query(F.data == "intent_business")
async def handle_business_intent(callback: CallbackQuery):
    # Сохранить intent, показать выбор цели дохода

@router.callback_query(F.data.startswith("pain_"))
async def handle_pain_selection(callback: CallbackQuery):
    # Сохранить боль, начать прогрев

@router.callback_query(F.data.startswith("income_"))
async def handle_income_selection(callback: CallbackQuery):
    # Сохранить цель, начать бизнес-прогрев
```

---

### ЭТАП 3: Реферальные ссылки (3-4 часа)

#### 3.1 Конфигурация

**Добавить в `.env`:**
```env
# Реферальные ссылки
NL_REFERRAL_ID=ТВОЙ_ID_ПАРТНЁРА
NL_REGISTRATION_LINK=https://nl-int.com/ref/ТВОЙ_ID
NL_SHOP_LINK=https://nl-int.com/shop?ref=ТВОЙ_ID
```

**Добавить в `shared/config/settings.py`:**
```python
# Referral settings
nl_referral_id: str = Field(default="", env="NL_REFERRAL_ID")
nl_registration_link: str = Field(default="", env="NL_REGISTRATION_LINK")
nl_shop_link: str = Field(default="", env="NL_SHOP_LINK")
```

#### 3.2 Генерация ссылок на продукты

**Новый файл: `curator_bot/utils/referral_links.py`**

```python
from shared.config.settings import settings

def get_product_link(product_id: str) -> str:
    """Генерирует реферальную ссылку на продукт"""
    return f"{settings.nl_shop_link}&product={product_id}"

def get_registration_link() -> str:
    """Возвращает ссылку на регистрацию партнёра"""
    return settings.nl_registration_link

def get_starter_kit_link() -> str:
    """Ссылка на стартовый набор"""
    return f"{settings.nl_shop_link}&category=starter"

# Маппинг продуктов на ID в магазине NL
PRODUCT_IDS = {
    "energy_diet_smart": "ed-smart-001",
    "energy_diet_hd": "ed-hd-001",
    "greenflash_vitamins": "gf-vit-001",
    "greenflash_collagen": "gf-col-001",
    "lovely_cosmetics": "lv-cos-001",
    # ... добавить все продукты
}
```

#### 3.3 Интеграция в сообщения

```python
# В funnels/messages.py

from curator_bot.utils.referral_links import get_product_link, get_registration_link

def get_product_recommendation(pain_point: str, goal: str) -> dict:
    """Возвращает рекомендацию продукта с ссылкой"""

    recommendations = {
        "weight": {
            "product": "Energy Diet Smart",
            "description": "Контроль веса без голода",
            "price": "3,500 ₽",
            "link": get_product_link("energy_diet_smart")
        },
        "energy": {
            "product": "Greenflash Витамины",
            "description": "Энергия и работоспособность",
            "price": "2,800 ₽",
            "link": get_product_link("greenflash_vitamins")
        },
        # ... другие рекомендации
    }

    return recommendations.get(pain_point)
```

---

### ЭТАП 4: Сбор контактов (4-5 часов)

#### 4.1 Запрос контакта

**Когда запрашивать:**
- После 3-го сообщения прогрева (если не кликнул "Заказать")
- Через 24 часа после последнего сообщения
- При попытке уйти из диалога

**Сценарий:**
```
Бот: "Кстати, могу прислать тебе полезный гайд:
      📚 '7 шагов к здоровому похудению'

      Куда отправить?"

[📱 На телефон (WhatsApp)]
[📧 На email]
[❌ Не нужно]
```

**После выбора "На телефон":**
```
Бот: "Отлично! Напиши свой номер телефона
      (в формате +7XXXXXXXXXX)"
```

#### 4.2 Изменения в БД

```sql
ALTER TABLE users ADD COLUMN phone VARCHAR(20);
ALTER TABLE users ADD COLUMN email VARCHAR(100);
ALTER TABLE users ADD COLUMN contact_collected_at TIMESTAMP;
ALTER TABLE users ADD COLUMN lead_status VARCHAR(50) DEFAULT 'new';
-- Статусы: new, contacted, interested, ordered, partner, inactive
```

#### 4.3 Уведомление владельцу

**Новый файл: `curator_bot/notifications/lead_alerts.py`**

```python
from aiogram import Bot
from shared.config.settings import settings

async def notify_hot_lead(bot: Bot, user_data: dict):
    """Отправляет уведомление о горячем лиде владельцу"""

    message = f"""
🔥 ГОРЯЧИЙ ЛИД!

👤 Имя: {user_data['first_name']}
📱 Телефон: {user_data['phone']}
📧 Email: {user_data['email']}

🎯 Интерес: {user_data['intent']}
💊 Боль: {user_data['pain_point']}
💰 Цель дохода: {user_data['income_goal']}

📊 Прошёл шагов воронки: {user_data['funnel_step']}
⏰ Время в боте: {user_data['time_in_funnel']}

👉 Telegram: @{user_data['username']}
"""

    await bot.send_message(
        chat_id=settings.admin_telegram_ids[0],
        text=message
    )
```

---

### ЭТАП 5: Автоматические напоминания (5-6 часов)

#### 5.1 Scheduler для напоминаний

**Новый файл: `curator_bot/scheduler/reminder_scheduler.py`**

```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from datetime import datetime, timedelta

scheduler = AsyncIOScheduler()

async def check_inactive_users():
    """Проверяет неактивных пользователей и отправляет напоминания"""

    async with AsyncSessionLocal() as session:
        # Пользователи, не активные 24 часа
        inactive_24h = await get_users_inactive_for(hours=24)
        for user in inactive_24h:
            await send_reminder_24h(user)

        # Пользователи, не активные 48 часов
        inactive_48h = await get_users_inactive_for(hours=48)
        for user in inactive_48h:
            await send_reminder_48h(user)

        # Пользователи, не активные 7 дней
        inactive_7d = await get_users_inactive_for(hours=168)
        for user in inactive_7d:
            await send_final_reminder(user)

def setup_reminder_scheduler():
    """Настраивает расписание напоминаний"""
    scheduler.add_job(
        check_inactive_users,
        'interval',
        hours=6,  # Проверять каждые 6 часов
        id='check_inactive_users'
    )
    scheduler.start()
```

#### 5.2 Тексты напоминаний

```python
# curator_bot/funnels/reminders.py

REMINDER_24H = """
Привет! 👋

Вчера мы говорили о {pain_point}.
Остались вопросы? Я всё ещё здесь и готов помочь.

{personalized_tip}

Напиши, если что-то интересует!
"""

REMINDER_48H = """
{first_name}, не пропадай!

У меня для тебя кое-что полезное:
🎁 Скидка 10% на первый заказ — действует ещё 24 часа

👉 Заказать: {product_link}

Или просто напиши — обсудим твои вопросы.
"""

REMINDER_7D = """
{first_name}, давно не общались!

Знаю, что времени всегда мало.
Но если вопрос {pain_point} всё ещё актуален — я здесь.

Один факт на подумать:
{interesting_fact}

Напиши "+" если хочешь продолжить разговор.
"""
```

---

### ЭТАП 6: CRM и аналитика воронки (6-8 часов)

#### 6.1 Дашборд статистики

**Новая команда: `/funnel_stats` (только для админа)**

```
📊 СТАТИСТИКА ВОРОНКИ

За сегодня:
├── Новых лидов: 47
├── Выбрали "Клиент": 32 (68%)
├── Выбрали "Бизнес": 12 (26%)
├── Просто смотрят: 3 (6%)
│
├── Дошли до предложения: 28 (60%)
├── Кликнули "Заказать": 8 (17%)
├── Оставили контакт: 15 (32%)
│
└── Конверсия в заказ: ~5% (оценка)

За неделю:
├── Новых лидов: 312
├── Заказов (примерно): 15-20
├── Новых партнёров: 3-5
└── Потенциальный доход: 15,000-25,000 ₽

🔥 Горячие лиды (требуют внимания): 8
```

#### 6.2 Новые таблицы БД

```sql
-- Таблица событий воронки
CREATE TABLE funnel_events (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    event_type VARCHAR(50),  -- start, intent_selected, pain_selected,
                             -- product_shown, link_clicked, contact_left
    event_data JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Индексы для аналитики
CREATE INDEX idx_funnel_events_type ON funnel_events(event_type);
CREATE INDEX idx_funnel_events_date ON funnel_events(created_at);

-- Таблица горячих лидов
CREATE TABLE hot_leads (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    score INTEGER DEFAULT 0,  -- Очки "горячести"
    last_action VARCHAR(100),
    notified_at TIMESTAMP,
    status VARCHAR(50) DEFAULT 'new'
);
```

#### 6.3 Скоринг лидов

```python
# curator_bot/analytics/lead_scoring.py

def calculate_lead_score(user: User, events: list) -> int:
    """Рассчитывает 'горячесть' лида"""
    score = 0

    # Базовые очки
    if user.user_intent == 'client':
        score += 10
    elif user.user_intent == 'business':
        score += 20  # Бизнес-лиды ценнее

    # За прохождение воронки
    score += user.funnel_step * 5

    # За оставленный контакт
    if user.phone or user.email:
        score += 30

    # За клики по ссылкам
    link_clicks = sum(1 for e in events if e.event_type == 'link_clicked')
    score += link_clicks * 10

    # За активность (сообщения)
    messages_count = sum(1 for e in events if e.event_type == 'message')
    score += min(messages_count * 2, 20)  # Макс 20 очков

    # Штраф за неактивность
    if user.last_activity:
        days_inactive = (datetime.now() - user.last_activity).days
        score -= days_inactive * 5

    return max(score, 0)

# Пороги:
# 0-20: Холодный
# 21-50: Тёплый
# 51-80: Горячий
# 81+: Очень горячий (уведомить владельца!)
```

---

## Структура новых файлов

```
curator_bot/
├── funnels/
│   ├── __init__.py
│   ├── client_funnel.py         # Воронка для клиентов
│   ├── business_funnel.py       # Воронка для бизнеса
│   ├── messages.py              # Все тексты сообщений
│   ├── keyboards.py             # Inline-клавиатуры
│   └── reminders.py             # Тексты напоминаний
│
├── utils/
│   ├── referral_links.py        # Генерация реф. ссылок
│   └── contact_collector.py     # Сбор контактов
│
├── scheduler/
│   └── reminder_scheduler.py    # Автонапоминания
│
├── notifications/
│   └── lead_alerts.py           # Уведомления о лидах
│
├── analytics/
│   ├── funnel_stats.py          # Статистика воронки
│   └── lead_scoring.py          # Скоринг лидов
│
└── handlers/
    ├── callbacks.py             # ОБНОВИТЬ: добавить callback воронки
    └── commands.py              # ОБНОВИТЬ: новый /start
```

---

## Изменения в существующих файлах

### 1. `curator_bot/handlers/commands.py`

**Изменить функцию `cmd_start`:**
- Добавить inline-кнопки выбора пути
- Сохранять начало воронки в БД

### 2. `curator_bot/handlers/callbacks.py`

**Добавить:**
- Обработчики выбора intent (клиент/бизнес)
- Обработчики выбора боли
- Обработчики выбора цели дохода
- Обработчики кнопок воронки

### 3. `curator_bot/database/models.py`

**Добавить поля в модель User:**
```python
user_intent = Column(String(50))        # client/business/curious
pain_point = Column(String(100))        # weight/energy/immunity...
income_goal = Column(String(50))        # 10-30k/50-100k/200k+
funnel_step = Column(Integer, default=0)
funnel_started_at = Column(DateTime)
phone = Column(String(20))
email = Column(String(100))
lead_status = Column(String(50), default='new')
lead_score = Column(Integer, default=0)
```

### 4. `curator_bot/main.py`

**Добавить:**
- Импорт и запуск reminder_scheduler
- Регистрацию новых роутеров

### 5. `.env`

**Добавить:**
```env
NL_REFERRAL_ID=
NL_REGISTRATION_LINK=
NL_SHOP_LINK=
```

---

## Миграции БД

**Создать файл: `scripts/migrate_funnel.py`**

```python
"""
Миграция БД для воронки продаж
"""
import asyncio
from sqlalchemy import text
from shared.database.base import engine

async def migrate():
    async with engine.begin() as conn:
        # Добавляем поля для воронки
        await conn.execute(text("""
            ALTER TABLE users
            ADD COLUMN IF NOT EXISTS user_intent VARCHAR(50),
            ADD COLUMN IF NOT EXISTS pain_point VARCHAR(100),
            ADD COLUMN IF NOT EXISTS income_goal VARCHAR(50),
            ADD COLUMN IF NOT EXISTS funnel_step INTEGER DEFAULT 0,
            ADD COLUMN IF NOT EXISTS funnel_started_at TIMESTAMP,
            ADD COLUMN IF NOT EXISTS phone VARCHAR(20),
            ADD COLUMN IF NOT EXISTS email VARCHAR(100),
            ADD COLUMN IF NOT EXISTS lead_status VARCHAR(50) DEFAULT 'new',
            ADD COLUMN IF NOT EXISTS lead_score INTEGER DEFAULT 0
        """))

        # Создаём таблицу событий воронки
        await conn.execute(text("""
            CREATE TABLE IF NOT EXISTS funnel_events (
                id SERIAL PRIMARY KEY,
                user_id INTEGER REFERENCES users(id),
                event_type VARCHAR(50),
                event_data JSONB,
                created_at TIMESTAMP DEFAULT NOW()
            )
        """))

        # Создаём таблицу горячих лидов
        await conn.execute(text("""
            CREATE TABLE IF NOT EXISTS hot_leads (
                id SERIAL PRIMARY KEY,
                user_id INTEGER REFERENCES users(id),
                score INTEGER DEFAULT 0,
                last_action VARCHAR(100),
                notified_at TIMESTAMP,
                status VARCHAR(50) DEFAULT 'new'
            )
        """))

        print("✅ Миграция воронки выполнена успешно!")

if __name__ == "__main__":
    asyncio.run(migrate())
```

---

## Оценка трудозатрат

| Этап | Задача | Часы | Приоритет |
|------|--------|------|-----------|
| 1 | Воронка квалификации | 8-10 | 🔴 Критично |
| 2 | Цепочки прогрева | 10-12 | 🔴 Критично |
| 3 | Реферальные ссылки | 3-4 | 🔴 Критично |
| 4 | Сбор контактов | 4-5 | 🟡 Важно |
| 5 | Автонапоминания | 5-6 | 🟡 Важно |
| 6 | CRM и аналитика | 6-8 | 🟢 Полезно |
| **ИТОГО** | | **36-45** | |

---

## Порядок реализации

### Неделя 1: MVP воронки (20-25 часов)
1. ✅ Этап 1: Воронка квалификации
2. ✅ Этап 3: Реферальные ссылки
3. ✅ Часть Этапа 2: Базовые цепочки (1-2 сообщения)

**Результат:** Бот квалифицирует лидов и даёт ссылки на заказ

### Неделя 2: Полные цепочки (15-20 часов)
1. ✅ Этап 2: Полные цепочки прогрева
2. ✅ Этап 4: Сбор контактов
3. ✅ Этап 5: Напоминания

**Результат:** Полноценная автоворонка с дожимом

### Неделя 3: Аналитика (6-8 часов)
1. ✅ Этап 6: CRM и статистика

**Результат:** Понимание конверсий и горячих лидов

---

## Ожидаемые результаты

### До доработки:
- Бот = справочник
- Конверсия в заказ: ~0% (нет призыва к действию)
- Конверсия в партнёра: ~0%

### После доработки:
- Бот = автоматическая воронка продаж
- Конверсия в заказ: 5-15% от лидов
- Конверсия в партнёра: 1-5% от лидов

### При 100,000 просмотров Reels/месяц:
- ~1,000-2,000 лидов в бот
- ~50-150 заказов
- ~10-50 новых партнёров
- **Доход: 20,000-100,000 ₽/мес**

---

## Следующие шаги

1. **Получить реферальные ссылки NL** — нужен твой ID партнёра
2. **Утвердить тексты сообщений** — можем адаптировать под твой стиль
3. **Определить продукты для рекомендаций** — какие продвигаем в первую очередь
4. **Начать реализацию с Этапа 1** — воронка квалификации

---

**Готов начать реализацию?**
